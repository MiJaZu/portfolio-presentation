---
import Particles from '../React/Particles';
import DarkThemeToggle from './DarkThemeToggle.astro';
---

<html lang="es" class="dark">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>Misael Zurita - Presentation</title>

    <script is:inline>
      const theme = localStorage.getItem('theme') || 'dark';
      document.documentElement.classList.toggle('dark', theme === 'dark');
    </script>
  </head>
  <body
    class="h-screen overflow-x-hidden transition-colors duration-300 bg-primary text-t-primary dark:bg-dark-primary dark:text-dark-t-primary"
  >
    <Particles client:only="preact" />
    <DarkThemeToggle />
    <slot />
  </body>
</html>
